service: midway-faas-examples

provider:
  name: aliyun

functions:
  index:
    handler: index.handler
    events:
      - schedule:
          type: every
          value: '0 0 8 * * *'
          payload: 'awesome-fc'

